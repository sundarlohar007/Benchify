apiVersion: v1
kind: Service
metadata:
  name: {{ include "benchify.fullname" . }}
  labels:
    {{- include "benchify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "benchify.selectorLabels" . | nindent 4 }}
