---
phase: 05-v3-0-game-engine-plugins-ios-injection-tvos-pc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - performancebench-injector/sdk/src/engine_core/mod.rs
  - performancebench-injector/sdk/src/engine_core/marker.rs
  - performancebench-injector/sdk/src/engine_core/auto_marker.rs
  - performancebench-injector/sdk/src/engine_core/metrics.rs
  - performancebench-injector/sdk/src/lib.rs
  - performancebench-injector/sdk/Cargo.toml
  - benchify-unity-plugin/package.json
  - benchify-unity-plugin/Editor/BenchifyEditorWindow.cs
  - benchify-unity-plugin/Editor/BenchifySettings.cs
  - benchify-unity-plugin/Runtime/BenchifyPlugin.cs
  - benchify-unity-plugin/Runtime/BeginMarker.cs
  - benchify-unity-plugin/Runtime/AutoMarkerHook.cs
  - benchify-unity-plugin/Runtime/NativeBindings.cs
  - benchify-unity-plugin/README.md
  - benchify-unreal-plugin/Benchify.uplugin
  - benchify-unreal-plugin/Source/Benchify/Benchify.Build.cs
  - benchify-unreal-plugin/Source/Benchify/Public/BenchifyBPLibrary.h
  - benchify-unreal-plugin/Source/Benchify/Public/BenchifySubsystem.h
  - benchify-unreal-plugin/Source/Benchify/Private/BenchifyBPLibrary.cpp
  - benchify-unreal-plugin/Source/Benchify/Private/BenchifySubsystem.cpp
  - benchify-unreal-plugin/Source/Benchify/Private/BenchifyNativeBridge.cpp
  - benchify-unreal-plugin/Source/BenchifyEditor/Public/BenchifyEditorWidget.h
  - benchify-unreal-plugin/Source/BenchifyEditor/Private/BenchifyEditorWidget.cpp
  - benchify-unreal-plugin/Source/BenchifyEditor/BenchifyEditor.Build.cs
  - benchify-godot-plugin/benchify_plugin.gdextension
  - benchify-godot-plugin/addons/benchify/plugin.cfg
  - benchify-godot-plugin/addons/benchify/benchify_autoload.gd
  - benchify-godot-plugin/addons/benchify/begin_marker.gd
  - benchify-godot-plugin/addons/benchify/editor_dock.gd
  - benchify-godot-plugin/addons/benchify/metrics_provider.gdextension
  - performancebench/lib/features/plugins/unified_installer_screen.dart
  - performancebench/lib/features/plugins/engine_detector.dart
  - performancebench/lib/features/plugins/plugin_install_card.dart
  - performancebench/lib/core/services/plugin_install_service.dart
autonomous: true
requirements:
  - V30-01
  - V30-02
  - V30-03
user_setup:
  - service: Unity Editor
    why: "Unity plugin development and testing"
    env_vars: []
    dashboard_config:
      - task: "Install Unity 2022.3 LTS or later"
        location: "https://unity.com/download"
  - service: Unreal Engine
    why: "Unreal plugin development and testing"
    env_vars: []
    dashboard_config:
      - task: "Install Unreal Engine 5.3 or later"
        location: "https://www.unrealengine.com/download"
  - service: Godot Engine
    why: "Godot plugin development and testing"
    env_vars: []
    dashboard_config:
      - task: "Install Godot 4.2 or later"
        location: "https://godotengine.org/download"

must_haves:
  truths:
    - "Unity developer installs UPM package, adds auto-markers on scene load, and sees draw calls + Mono memory + GC alloc in Editor window during Play mode"
    - "Unreal developer installs plugin from Plugins/ folder, uses BeginMarker Blueprint node, and sees RHI frame time + GPU stats in editor Slate widget"
    - "Godot developer enables addon, gets Autoload singleton with auto-markers on scene change, and sees RenderingServer draw calls in editor dock"
    - "PerformanceBench desktop app detects installed engines and offers one-click plugin install to Unity/Unreal/Godot project directories"
  artifacts:
    - path: "performancebench-injector/sdk/src/engine_core/mod.rs"
      provides: "Shared Rust library: auto-marker state machine + metric collector bridge, reused by all three engine wrappers"
      contains: "pub mod marker"
    - path: "benchify-unity-plugin/Runtime/BenchifyPlugin.cs"
      provides: "Unity C# MonoBehaviour: connects to Rust .so via P/Invoke, exposes PerformanceBench.BeginMarker(), reports Unity stats (draw calls, batches, SetPass, Mono memory, GC alloc) each frame"
      contains: "PerformanceBench.BeginMarker"
    - path: "benchify-unreal-plugin/Source/Benchify/Public/BenchifyBPLibrary.h"
      provides: "Unreal C++ BlueprintFunctionLibrary: exposes UPerformanceBenchBPLibrary::BeginMarker node, auto-marker delegate on PostLoadMapWithWorld"
      contains: "BeginMarker"
    - path: "benchify-godot-plugin/addons/benchify/benchify_autoload.gd"
      provides: "Godot GDScript Autoload singleton: auto-marker on SceneTree.scene_changed, RenderingServer draw call queries, BeginMarker API"
      contains: "extends Node"
    - path: "performancebench/lib/features/plugins/unified_installer_screen.dart"
      provides: "Desktop Flutter screen: scans filesystem for Unity/Unreal/Godot projects, shows detected engines with install/remove toggle per project"
      contains: "EngineDetector"
  key_links:
    - from: "benchify-unity-plugin/Runtime/NativeBindings.cs"
      to: "performancebench-injector/sdk/src/engine_core/"
      via: "P/Invoke to Rust .so"
      pattern: "DllImport.*benchify_engine"
    - from: "benchify-unreal-plugin/Source/Benchify/Private/BenchifyNativeBridge.cpp"
      to: "performancebench-injector/sdk/src/engine_core/"
      via: "C++ FFI extern \"C\""
      pattern: "extern.*C.*benchify_engine"
    - from: "benchify-godot-plugin/addons/benchify/metrics_provider.gdextension"
      to: "performancebench-injector/sdk/src/engine_core/"
      via: "GDExtension Foreign Interface"
      pattern: "benchify_engine"
    - from: "performancebench/lib/features/plugins/unified_installer_screen.dart"
      to: "benchify-unity-plugin, benchify-unreal-plugin, benchify-godot-plugin"
      via: "File I/O copy to project directories"
      pattern: "Directory.*Assets"
---

<objective>
Create the shared Rust core library for game engine profiling and three per-engine plugin wrappers (Unity UPM, Unreal C++/Blueprint, Godot GDScript) with auto-marker APIs, editor stats dashboards, and a desktop unified installer.

Purpose: Game developers get engine-native profiling (BeginMarker API, draw calls, memory, GPU stats) without leaving their editor. The shared Rust core ensures consistent metric logic across all three engines per D-01.
Output: Three distributable engine plugins + shared Rust core crate + desktop unified installer UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-v3-0-game-engine-plugins-ios-injection-tvos-pc/05-CONTEXT.md
@UNIFIED-SPEC.md §32-36 (Game engine plugins)
@performancebench-injector/sdk/src/ (existing Rust SDK — reuse metric modules)

<interfaces>
<!-- Key types and contracts the executor needs from the existing codebase -->

From performancebench-injector/sdk/src/models.rs:
```rust
pub struct MetricSample {
    pub session_id: String,
    pub timestamp: i64,
    pub fps: Option<f64>,
    pub jank_count: Option<i32>,
    pub cpu_system_pct: Option<f64>,
    pub cpu_app_pct: Option<f64>,
    pub memory_pss_kb: Option<i32>,
    pub memory_java_kb: Option<i32>,
    pub memory_native_kb: Option<i32>,
    pub memory_graphics_kb: Option<i32>,
    pub gpu_pct: Option<f64>,
    pub gpu_freq_mhz: Option<f64>,
    pub gpu_mem_kb: Option<i32>,
    // ... full model with ~50 fields
}
```

From performancebench-injector/sdk/src/transport.rs:
```rust
pub fn start_server() -> TcpListener  // Binds 127.0.0.1:8080, JSON newline-delimited
pub fn set_session_id(id: &str)
pub fn queue_sample(sample: MetricSample)
pub fn queue_event(event_json: &str)
```

From performancebench-injector/sdk/src/metrics/mod.rs:
```rust
pub mod fps;
pub mod cpu;
pub mod memory;
pub mod network;
pub mod gpu;
```

From performancebench-injector/sdk/src/lib.rs:
```rust
pub mod models;
pub mod transport;
pub mod metrics;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared Rust engine core library + Unity UPM plugin</name>
  <files>
    performancebench-injector/sdk/src/engine_core/mod.rs
    performancebench-injector/sdk/src/engine_core/marker.rs
    performancebench-injector/sdk/src/engine_core/auto_marker.rs
    performancebench-injector/sdk/src/engine_core/metrics.rs
    performancebench-injector/sdk/src/lib.rs
    performancebench-injector/sdk/Cargo.toml
    benchify-unity-plugin/package.json
    benchify-unity-plugin/Editor/BenchifyEditorWindow.cs
    benchify-unity-plugin/Editor/BenchifySettings.cs
    benchify-unity-plugin/Runtime/BenchifyPlugin.cs
    benchify-unity-plugin/Runtime/BeginMarker.cs
    benchify-unity-plugin/Runtime/AutoMarkerHook.cs
    benchify-unity-plugin/Runtime/NativeBindings.cs
    benchify-unity-plugin/README.md
  </files>
  <behavior>
    - Test 1: engine_core::marker::begin("test") creates marker entry, end() finalizes it with duration
    - Test 2: engine_core::auto_marker::scene_loaded("MainMenu") triggers marker pair
    - Test 3: engine_core::metrics::collect_unity_stats() returns draw_calls, batches, setpass_calls, mono_heap_kb, gc_alloc_kb
    - Test 4: Unity BeginMarker("boss_fight") creates scoped marker that auto-ends on Dispose
  </behavior>
  <action>
    **Part A — Shared Rust core library** (per D-01):
    Create `performancebench-injector/sdk/src/engine_core/` crate module with three submodules:

    1. `marker.rs` — Scoped marker state machine:
       - `pub struct ScopedMarker { name: String, start_ts: i64, end_ts: Option<i64> }`
       - `pub fn begin_marker(name: &str) -> ScopedMarker` — captures `SystemTime::now()` as start
       - `pub fn end_marker(marker: &mut ScopedMarker)` — sets end_ts
       - `pub fn marker_event_json(marker: &ScopedMarker) -> String` — serializes to `{"type":"marker","name":"...","start_ms":...,"duration_ms":...}`
       - Thread-safe via `Mutex<Vec<ScopedMarker>>` for concurrent use from engine main threads.

    2. `auto_marker.rs` — Auto-marker triggers:
       - `pub fn on_scene_load(scene_name: &str)` — emits begin/end marker pair for scene transition
       - `pub fn on_app_start()` — emits "App Launch" marker
       - Calls `transport::queue_event()` to push to TCP stream (matches Phase 4 Marker pattern).

    3. `metrics.rs` — Engine-specific metric collectors:
       - `pub struct UnityFrameStats { draw_calls: i32, batches: i32, setpass_calls: i32, mono_heap_kb: i64, gc_alloc_kb: i64, triangles: i32, vertices: i32 }`
       - `pub struct UnrealFrameStats { rhi_frame_time_ms: f64, draw_primitive_calls: i32, gpu_frame_time_ms: f64, stat_unit_json: String }`
       - `pub struct GodotFrameStats { draw_calls: i32, objects_drawn: i32, material_changes: i32, shader_changes: i32, surface_changes: i32, verts_drawn: i32 }`
       - Each struct impls `fn to_json(&self) -> String` for TCP streaming.

    Register `pub mod engine_core;` in `performancebench-injector/sdk/src/lib.rs`.
    Add serde/serde_json as Cargo.toml deps if not present.

    **Part B — Unity UPM plugin** (per D-02, D-03, D-05):

    Create `benchify-unity-plugin/` directory with standard UPM package structure:

    1. `package.json`:
       - `"name": "dev.benchify.unity-plugin"`, version `"3.0.0"`
       - `"unity": "2022.3"`, `"displayName": "Benchify Profiler"`
       - `"description": "Free, open-source performance profiler for Unity games. Auto-markers, draw calls, memory, FPS overlay during Play mode."`
       - MIT license

    2. `Runtime/BenchifyPlugin.cs` — Main MonoBehaviour:
       - Singleton pattern via `[RuntimeInitializeOnLoadMethod]`
       - In `Start()`: bind to `SceneManager.sceneLoaded` for auto-markers (per D-01 scene auto-marker)
       - In `Update()`: sample `UnityEngine.Profiling.Profiler` stats each frame:
         - Draw calls: `UnityStats.drawCalls`
         - Batches: `UnityStats.batches`
         - SetPass calls: `UnityStats.setPassCalls`
         - Mono heap: `Profiler.GetMonoHeapSizeLong()`
         - GC alloc: `Profiler.GetTotalAllocatedMemoryLong() - previous`
         - Triangles/vertices: `UnityStats.triangles`, `UnityStats.vertices`
       - Each second, aggregate frame deltas into `MetricSample`-compatible JSON and write to `engine_core` via `NativeBindings`.
       - `public static void BeginMarker(string name)` — calls `engine_core::marker::begin_marker`
       - Conditional compilation: wrap all Benchify calls in `#if PERFORMANCE_BENCH` to ensure zero overhead in release builds when stripped.

    3. `Runtime/BeginMarker.cs` — Scoped marker helper:
       - Implements `IDisposable`
       - `public BeginMarker(string name)` calls `BenchifyPlugin.BeginMarker(name)`
       - `Dispose()` calls `BenchifyPlugin.EndMarker()`
       - Usage: `using(new BeginMarker("boss_fight")) { ... }` (matches D-02 pattern exactly)

    4. `Runtime/AutoMarkerHook.cs` — Scene load hook:
       - Static class subscribing to `SceneManager.sceneLoaded`
       - On scene load: creates auto-marker with scene name
       - Handles additive scene loading (multiple scenes) — labels marker as "Additive:SceneName"

    5. `Runtime/NativeBindings.cs` — P/Invoke to Rust .so (per D-01):
       - `[DllImport("benchify_engine")] private static extern void benchify_engine_begin_marker(string name);`
       - `[DllImport("benchify_engine")] private static extern void benchify_engine_end_marker();`
       - `[DllImport("benchify_engine")] private static extern void benchify_engine_queue_frame_stats(string json);`
       - Handles per-platform .so naming: `benchify_engine.dll` (Win), `libbenchify_engine.dylib` (Mac), `libbenchify_engine.so` (Linux)
       - For editor Play mode, the native lib is loaded from `Assets/Plugins/x86_64/` or the UPM package's `~/.benchify/` cache.

    6. `Editor/BenchifyEditorWindow.cs` — Editor stats window (per D-03):
       - Opens via `Window > Benchify Profiler` menu
       - `EditorWindow` with `OnGUI()` using `EditorGUILayout`:
         - FPS (calculated from Time.deltaTime over rolling 1s)
         - Draw calls / batches / SetPass (live from `UnityStats` during Play mode)
         - Mono heap size (MB, with progress bar at 1GB scale)
         - GC alloc per frame (KB, red if > 1MB)
       - Read-only stats display — no profiling controls (profiling still via desktop app per D-03)
       - Auto-refreshes in Play mode via `EditorApplication.update` callback
       - "Open PerformanceBench Desktop" button (launches desktop app via `System.Diagnostics.Process`)

    7. `Editor/BenchifySettings.cs` — Settings Provider:
       - `ScriptableObject` with `SettingsProvider` attribute
       - Settings: TCP port (default 8080), auto-marker toggle, stats refresh interval
       - Accessible via `Edit > Project Settings > Benchify`

    8. `README.md`:
       - Install via git URL in UPM: `https://github.com/sundarlohar007/Benchify.git?path=/benchify-unity-plugin`
       - Quick-start: add `using(new BeginMarker("boss_fight"))` around boss spawn
       - Link to PerformanceBench desktop app download

    **Threat model considerations:**
    - NativeBindings.cs P/Invoke only loads from known paths (no `PATH` search)
    - Editor window is read-only — no write access to profiling data
    - `#if PERFORMANCE_BENCH` guard ensures production builds have zero Benchify code retained (no IL2CPP stripping needed)
  </action>
  <verify>
    <automated>cd performancebench-injector/sdk && cargo test engine_core</automated>
  </verify>
  <done>
    - `cargo test engine_core` passes (marker state machine + auto-marker + metric structs tested)
    - `benchify-unity-plugin/package.json` exists with valid UPM manifest
    - `BenchifyPlugin.cs` compiles against Unity 2022.3 API references (Editor window, UnityStats, Profiler)
    - Directory structure matches standard UPM package layout (Runtime/, Editor/, package.json)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unreal Engine + Godot plugins</name>
  <files>
    benchify-unreal-plugin/Benchify.uplugin
    benchify-unreal-plugin/Source/Benchify/Benchify.Build.cs
    benchify-unreal-plugin/Source/Benchify/Public/BenchifyBPLibrary.h
    benchify-unreal-plugin/Source/Benchify/Public/BenchifySubsystem.h
    benchify-unreal-plugin/Source/Benchify/Private/BenchifyBPLibrary.cpp
    benchify-unreal-plugin/Source/Benchify/Private/BenchifySubsystem.cpp
    benchify-unreal-plugin/Source/Benchify/Private/BenchifyNativeBridge.cpp
    benchify-unreal-plugin/Source/BenchifyEditor/Public/BenchifyEditorWidget.h
    benchify-unreal-plugin/Source/BenchifyEditor/Private/BenchifyEditorWidget.cpp
    benchify-unreal-plugin/Source/BenchifyEditor/BenchifyEditor.Build.cs
    benchify-unreal-plugin/README.md
    benchify-godot-plugin/benchify_plugin.gdextension
    benchify-godot-plugin/addons/benchify/plugin.cfg
    benchify-godot-plugin/addons/benchify/benchify_autoload.gd
    benchify-godot-plugin/addons/benchify/begin_marker.gd
    benchify-godot-plugin/addons/benchify/editor_dock.gd
    benchify-godot-plugin/addons/benchify/metrics_provider.gdextension
    benchify-godot-plugin/README.md
  </files>
  <behavior>
    - Test 1: Unreal UBenchifyBPLibrary::BeginMarker("boss") creates scoped marker that ends at scope exit
    - Test 2: Unreal BenchifySubsystem fires auto-marker on PostLoadMapWithWorld delegate
    - Test 3: Godot BeginMarker.new("boss") creates scoped marker using `with` pattern
    - Test 4: Godot Autoload fires auto-marker on SceneTree.scene_changed signal
    - Test 5: Godot metrics_provider captures RenderingServer.get_rendering_info() draw call counts
  </behavior>
  <action>
    **Part A — Unreal Engine Plugin** (per D-02, D-03):

    Standard Unreal Engine plugin structure under `benchify-unreal-plugin/`:

    1. `Benchify.uplugin` — Plugin descriptor (UE 5.3+):
       - `"FriendlyName": "Benchify Profiler"`
       - `"VersionName": "3.0.0"`, MIT license
       - Modules: `Benchify` (Runtime, LoadingPhase: Default), `BenchifyEditor` (Editor, LoadingPhase: PostEngineInit)

    2. `Source/Benchify/Benchify.Build.cs` — Build rules:
       - Public dependencies: Core, CoreUObject, Engine, Slate, SlateCore
       - Private dependencies: (none extra)
       - `PublicAdditionalLibraries.Add()` for Rust .so per platform (Win/Mac/Linux paths)
       - `RuntimeDependencies.Add()` to copy .so to packaged build output

    3. `Source/Benchify/Public/BenchifySubsystem.h` — UEngineSubsystem:
       - Inherits `UEngineSubsystem` — auto-instantiated by engine
       - `virtual void Initialize(FSubsystemCollectionBase& Collection) override` — binds to `FCoreUObjectDelegates::PostLoadMapWithWorld` for auto-markers (per D-01)
       - `virtual void Deinitialize() override`
       - `void OnMapLoaded(UWorld* World)` — creates auto-marker with world map name
       - `void Tick(float DeltaTime)` — collects per-frame RHI stats:
         - Frame time: `FApp::GetDeltaTime()` * 1000 → ms
         - RHI stats: `GDynamicRHI->RHIGetGPUFrameCycles()` for GPU time
         - Draw calls + primitives: via `GOptionallyCompressedPlatformData` stats
       - Aggregates per-second and JSON-serializes into `engine_core::metrics::UnrealFrameStats`

    4. `Source/Benchify/Public/BenchifyBPLibrary.h` — Blueprint Function Library:
       - `UCLASS()` with `UFUNCTION(BlueprintCallable)`
       - `static void BeginMarker(const FString& Name)` — scoped marker entry (per D-02)
       - `static void EndMarker()` — scoped marker exit
       - `static FString GetFrameStatsJson()` — returns current frame stats as JSON string
       - Blueprint-accessible: `Category = "Benchify|Profiling"`

    5. `Source/Benchify/Private/BenchifyBPLibrary.cpp`:
       - `BeginMarker`: pushes `FScopedMarker` onto thread-local stack, calls `benchify_engine_begin_marker(TCHAR_TO_UTF8(*Name))`
       - `EndMarker`: pops stack, calls `benchify_engine_end_marker()`
       - `GetFrameStatsJson`: reads `BenchifySubsystem` latest frame stats, returns JSON

    6. `Source/Benchify/Private/BenchifyNativeBridge.cpp` — C FFI bridge (per D-01):
       - `extern "C"` wrapper calling Rust `engine_core` FFI exports:
       - `benchify_engine_begin_marker(const char* name)`
       - `benchify_engine_end_marker()`
       - `benchify_engine_queue_frame_stats(const char* json)`
       - Platform-specific `#if PLATFORM_WINDOWS` / `#elif PLATFORM_MAC` / `#elif PLATFORM_LINUX` for .dll/.dylib/.so loading

    7. `Source/BenchifyEditor/` — Editor customization (per D-03):
       - `BenchifyEditorWidget.h/.cpp` — Slate `SCompoundWidget`:
         - Shows: RHI frame time (ms), GPU frame time (ms), draw primitive calls, Stat Unit values
         - Read-only, auto-refresh during PIE (Play In Editor)
       - `BenchifyEditor.Build.cs` depends on UnrealEd, EditorStyle, Slate
       - Register via `FBenchifyEditorModule::StartupModule()` → `FGlobalTabmanager::Get()->RegisterNomadTabSpawner`
       - Accessible via `Window > Benchify Profiler` menu
       - Minimized widget shows current FPS badge (green/yellow/red) per Phase 4 overlay pattern

    8. `README.md`:
       - Install: clone into `[Project]/Plugins/Benchify/`
       - Usage: Blueprint node `BeginMarker` in any Blueprint
       - C++: `#include "BenchifyBPLibrary.h"` then `UBenchifyBPLibrary::BeginMarker(TEXT("MyMarker"))`

    **Part B — Godot Plugin** (per D-02, D-03, D-05):

    Godot 4.2+ addon under `benchify-godot-plugin/`:

    1. `benchify_plugin.gdextension` — GDExtension manifest:
       - Entry for `benchify_engine` library (Rust .so compiled as GDExtension)
       - Platform paths: `"windows.x86_64": "res://addons/benchify/bin/libbenchify_engine.dll"`, etc.
       - Compatibility: Godot 4.2 minimum

    2. `addons/benchify/plugin.cfg`:
       - `[plugin] name="Benchify Profiler"`, `version="3.0.0"`, `author="Benchify"`
       - `description="Free, open-source performance profiler for Godot games"`
       - `script="benchify_autoload.gd"` as Autoload singleton

    3. `addons/benchify/benchify_autoload.gd` — Autoload singleton (per D-01):
       - `extends Node` with class_name `Benchify`
       - `func _ready()`: connect to `get_tree().scene_changed` signal for auto-markers (per D-01)
       - Auto-marker: on scene change, emits `begin_marker(scene_name)`, captures scene file path
       - `_process(delta)`: samples metrics each frame:
         - Draw calls: `RenderingServer.get_rendering_info(RenderingServer.RENDERING_INFO_TOTAL_OBJECTS_IN_FRAME)`
         - Objects drawn, material changes, shader changes, surface changes, vertices drawn
         - All via `RenderingServer.RENDERING_INFO_*` enum queries (Godot 4.2+)
       - 1Hz aggregation: frame deltas → rolling FPS, per-second stat JSON → calls Rust engine_core via `metrics_provider`
       - API: `func begin_marker(name: String)` / `func end_marker()` (static methods on Autoload)
       - Thread-safe: all calls from main thread (Godot API constraint)

    4. `addons/benchify/begin_marker.gd` — Scoped marker pattern (per D-02):
       - `class_name BeginMarker extends RefCounted`
       - `var _name: String`
       - `func _init(name: String): _name = name; Benchify.begin_marker(name)`
       - `func _notification(what): if what == NOTIFICATION_PREDELETE: Benchify.end_marker()`
       - Usage: `with BeginMarker.new("boss_fight"):` — cleanup on scope exit via Godot `with` pattern

    5. `addons/benchify/editor_dock.gd` — Editor dock (per D-03):
       - `@tool extends EditorPlugin` class
       - Registers dock at bottom panel: `add_control_to_bottom_panel(dock, "Benchify")`
       - Dock UI (`Control` with VBoxContainer):
         - FPS label (green/yellow/red color per Phase 4 overlay)
         - Draw calls (RenderingServer query during editor play)
         - Objects/vertices/material changes per frame
         - "Open Desktop App" button
       - Read-only — no profiling controls
       - Auto-refreshes via `_process` timer at 2Hz

    6. `addons/benchify/metrics_provider.gdextension`:
       - Secondary GDExtension loading the same `libbenchify_engine` for metrics collection
       - Exposes `collect_frame_stats(json_string: String)` to GDScript

    7. `README.md`:
       - Install: copy `addons/benchify/` to project, enable in `Project > Project Settings > Plugins`
       - Quick-start: `Benchify.begin_marker("combat_start")` anywhere in GDScript
       - Godot Asset Library listing URL placeholder

    **Per-engine distro (D-05):** All three plugins include MIT license headers, no paid dependencies. Unity via UPM git URL, Unreal via GitHub clone into Plugins/, Godot via Asset Library or GitHub clone.

    **Threat model considerations:**
    - Unreal NativeBridge only loads .dll/.dylib/.so from known engine plugin paths (no `PATH` search, no `LoadLibraryW` with user paths)
    - Godot GDExtension loads from `res://addons/benchify/bin/` only — project-local, no system paths
    - Editor docks are read-only — no profiling data modification
  </action>
  <verify>
    <automated>
MISSING — V30-02 and V30-03 plugin validation requires respective engine installations. After file creation:
Verify via grep: grep -r "BeginMarker" benchify-unreal-plugin/ benchify-godot-plugin/
Verify plugin manifests: cat benchify-unreal-plugin/Benchify.uplugin benchify-godot-plugin/addons/benchify/plugin.cfg
    </automated>
  </verify>
  <done>
    - Unreal `.uplugin` descriptor valid with Runtime + Editor modules
    - `BenchifyBPLibrary.h` exported with `UFUNCTION(BlueprintCallable) static void BeginMarker(FString)`
    - `BenchifySubsystem` binds to `PostLoadMapWithWorld` delegate for auto-markers
    - Godot `plugin.cfg` enables `benchify_autoload.gd` as Autoload singleton
    - Godot `BeginMarker` class supports `with BeginMarker.new("name"):` pattern
    - Godot editor dock queries `RenderingServer.RENDERING_INFO_*` for draw call metrics
    - All source files have MIT SPDX headers
  </done>
</task>

<task type="auto">
  <name>Task 3: Desktop unified installer</name>
  <files>
    performancebench/lib/features/plugins/unified_installer_screen.dart
    performancebench/lib/features/plugins/engine_detector.dart
    performancebench/lib/features/plugins/plugin_install_card.dart
    performancebench/lib/core/services/plugin_install_service.dart
  </files>
  <action>
    **Desktop Flutter unified installer** (per D-04, D-05):

    Add to `performancebench/lib/features/plugins/` — new "Plugins" section in desktop app.

    1. `engine_detector.dart` — Engine project scanner:
       - Scans common project directories (Documents, ~/projects, ~/dev, custom path from Settings)
       - Detection heuristics:
         - Unity: Directory contains `Assets/`, `ProjectSettings/`, `Packages/manifest.json`
         - Unreal: Directory contains `*.uproject` file, `Source/` directory
         - Godot: Directory contains `project.godot` file
       - Returns `List<DetectedEngine>` with: `{path, engineType, projectName, hasPluginInstalled, pluginVersion}`
       - Checks `hasPluginInstalled` by looking for Benchify plugin files in project:
         - Unity: `Packages/dev.benchify.unity-plugin/` in manifest.json dependencies
         - Unreal: `Plugins/Benchify/Benchify.uplugin` exists
         - Godot: `addons/benchify/plugin.cfg` exists AND is enabled in project.godot
       - Runs in `Isolate` to avoid blocking UI during filesystem scan

    2. `plugin_install_card.dart` — Per-engine install widget:
       - Shows engine icon + project path + name
       - Status: "Not Installed" (gray) / "Installed v3.0.0" (green) / "Update Available" (amber)
       - Install button: copies plugin files to project directory
       - Remove button: deletes plugin files from project (with confirmation dialog)
       - Progress indicator during install
       - For Unity: also modifies `Packages/manifest.json` to add git URL dependency
       - For Godot: also modifies `project.godot` to enable the addon in `[addons]` section

    3. `plugin_install_service.dart` — File copy + config patching:
       - `Future<bool> installPlugin(DetectedEngine engine)` — main entry point
       - Unity install path:
         - Adds `"dev.benchify.unity-plugin": "https://github.com/sundarlohar007/Benchify.git?path=/benchify-unity-plugin"` to `manifest.json` dependencies
         - Alternatively: copies from bundled plugin directory if offline
       - Unreal install:
         - Copies `benchify-unreal-plugin/` to `[ProjectPath]/Plugins/Benchify/`
         - Also copies compiled Rust .dll/.dylib/.so to `Plugins/Benchify/Binaries/[Platform]/`
       - Godot install:
         - Copies `addons/benchify/` to `[ProjectPath]/addons/benchify/`
         - Modifies `project.godot` to add `benchify/benchify_autoload.gd` under `[autoload]` section
       - `Future<bool> removePlugin(DetectedEngine engine)` — reverses install (cleanup)
       - `Future<String?> getInstalledVersion(DetectedEngine engine)` — reads plugin.cfg/manifest for version
       - Error handling: file permission errors, project open in editor (warning, not error), disk space check

    4. `unified_installer_screen.dart` — Main screen:
       - Part of desktop app navigation (new "Plugins" nav item)
       - Top section: "Scan for Game Engines" button + custom path text field
       - Results list: `ListView.builder` of `PluginInstallCard` widgets
       - Empty state: "No game engine projects detected. Click Scan to search, or add a custom directory path."
       - "Install All" batch button (per D-04 — one-click install to all detected engines)
       - Refresh button to re-scan after install
       - Tab bar for filtering: All / Unity / Unreal / Godot
       - Section at bottom: "Standalone Distribution" — links to per-engine standard install methods (D-05 fallback):
         - Unity: "UPM git URL: https://github.com/.../benchify-unity-plugin"
         - Unreal: "Clone into your project's Plugins/Benchify/ folder"
         - Godot: "Available on Godot Asset Library (search: Benchify)"
       - Copy-to-clipboard buttons for each distribution URL

    **Threat model considerations:**
    - Engine detection only reads filesystem metadata (directory listing, file existence) — no file content exfiltration
    - Install writes only to user-specified project directories
    - manifest.json and project.godot edits are backed up before modification (`manifest.json.bak`)
    - No network calls during scan — fully offline file detection
  </action>
  <verify>
    <automated>
cd performancebench && dart analyze lib/features/plugins/ lib/core/services/plugin_install_service.dart
</automated>
  </verify>
  <done>
    - `engine_detector.dart` detects Unity projects (has `Assets/` + `ProjectSettings/`), Unreal projects (has `*.uproject`), Godot projects (has `project.godot`)
    - `plugin_install_service.dart` installs plugin files to correct engine project paths
    - `unified_installer_screen.dart` renders with scan button, detected engine list, and install/remove actions
    - Standalone distribution links (UPM URL, GitHub clone path, Asset Library) visible at bottom of screen
    - `dart analyze` passes with 0 errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unity Editor ↔ Rust .so | C# P/Invoke boundary — untrusted native code loaded |
| Unreal Engine ↔ Rust .so | C++ FFI boundary — dynamic library loading |
| Godot Editor ↔ GDExtension | Foreign interface boundary |
| Desktop installer ↔ Engine project files | File I/O boundary — writing to user's project directories |
| Engine plugin ↔ TCP port 8080 | JSON streaming to desktop app (localhost only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | NativeBindings.cs P/Invoke | mitigate | Load .so/.dll from known Unity Plugin paths only (not PATH env var). Validate library hash before loading. |
| T-05-02 | Tampering | plugin_install_service.dart | mitigate | Backup manifest.json/project.godot before editing. Validate file copy integrity via byte count check. |
| T-05-03 | Information Disclosure | Editor windows (Unity/Unreal/Godot) | accept | Editor windows display read-only profiling stats — no PII, no session data, no network transmission. Local display only. |
| T-05-04 | Elevation of Privilege | GDExtension loading | mitigate | Load from `res://addons/benchify/bin/` only — project-local, no system paths. |
| T-05-05 | Denial of Service | Auto-marker hooks | mitigate | Auto-marker callbacks are non-blocking; if native lib unavailable, plugin degrades gracefully (skip marker, log warning). `#if PERFORMANCE_BENCH` ensures zero impact in stripped builds. |
</threat_model>

<verification>
- `cargo test engine_core` passes all marker + metric struct tests
- `dart analyze` passes on desktop plugin files
- All plugin manifest files valid (package.json, .uplugin, plugin.cfg, .gdextension)
- All source files have MIT SPDX headers
</verification>

<success_criteria>
1. Unity developer can add UPM package via git URL, open `Window > Benchify Profiler`, and see draw calls + Mono memory during Play mode
2. Unreal developer can clone plugin into `Plugins/Benchify/`, access `BeginMarker` Blueprint node, and open Benchify editor tab showing RHI stats
3. Godot developer can enable addon, use `with BeginMarker.new("name"):` in GDScript, and see RenderingServer stats in editor dock
4. Desktop app "Plugins" screen detects installed engines and provides one-click install
</success_criteria>

<output>
After completion, create `.planning/phases/05-v3-0-game-engine-plugins-ios-injection-tvos-pc/05-01-SUMMARY.md`
</output>
